{
  "name": "@nadie/relayer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Nadie transaction relayer: pays gas for signed ConsentRegistry grants and revocations only.",
  "license": "MIT",
  "engines": {
    "node": ">=20.19.0"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsx --env-file=../../.env src/server.ts",
    "regenerate:abi": "node scripts/regenerate-abi.mjs",
    "start": "node --env-file=../../.env dist/server.js",
    "test": "vitest run",
    "typecheck": "tsc -b --noEmit false && tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@hono/node-server": "2.1.1",
    "hono": "4.13.7",
    "viem": "2.56.3",
    "zod": "4.6.2"
  },
  "devDependencies": {
    "@types/node": "22.20.2",
    "tsx": "4.23.13",
    "vitest": "3.2.7"
  }
}
