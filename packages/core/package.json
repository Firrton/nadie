{
  "name": "@nadie/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Nadie core package skeleton. Domain contracts arrive in Goal 2.",
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "node --test",
    "typecheck": "tsc -b --noEmit false && tsc --noEmit -p tsconfig.json"
  }
}